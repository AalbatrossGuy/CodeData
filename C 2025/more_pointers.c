#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void double_pointers();

int main() {
    double_pointers();
    return 0;
}

void double_pointers() {
   // When a pointer holds the address of another pointer, that's known as a pointer-to-pointer or double pointer.
}